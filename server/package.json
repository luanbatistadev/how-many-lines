{
  "name": "server",
  "type": "commonjs",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "14.x"
  },
  "license": "MIT",
  "scripts": {
    "build": "npm install && cd ../core && npm install && npx tsc",
    "start": "sls offline start"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.5.1",
    "how-many-lines": "../core",
    "node-fetch": "2",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "serverless-offline": "^8.3.1"
  }
}
